define([
  "vue", "text!templates/map-detail.vue"
], function(Vue, Template) {
  
  // The constructor for our component
  return function() {
    
    // The component to create
    var component = {
    };
        
    // Asign the component into the new object
    Object.assign(this, component);
  };
});
